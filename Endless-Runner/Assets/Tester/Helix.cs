using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Helix : MonoBehaviour
{
   

    //void Start()
    //{
    //    movement = transform.position;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    transform.position.x = unit * Math.Cos(Time.time * freq);
    //    transform.position.y = unit * Math.Sin(Time.time * freq);
    //    unit += Time.deltaTime;

    //}
}
